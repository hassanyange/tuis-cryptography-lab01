# Лабораторная работа №1
## Шифры простой замены

## Выполнил: Янге Хассан Кулва
## Группа: НФИмд-02-25
## Дата: 27/09/2025
## студенческий билет:1032255540

---
---

## Введение

В данной лабораторной работе были реализованы и исследованы два классических шифра простой замены: **шифр Цезаря** и **шифр Атбаш**. Шифры простой замены основаны на принципе замены каждого символа открытого текста на соответствующий символ шифротекста по фиксированному правилу.

## Теоретическая часть

### 1. Шифр Цезаря

Шифр Цезаря — это моноалфавитный шифр подстановки, в котором каждая буква открытого текста заменяется на букву, находящуюся на фиксированном числе позиций левее или правее неё в алфавите.

**Математическая модель:**
\[ T_k(a) = (a + k) \mod m \]
где:
- \(a\) — номер буквы в алфавите (0-31 для русского алфавита)
- \(k\) — ключ (величина сдвига)
- \(m\) — мощность алфавита (32 для русского)

### 2. Шифр Атбаш

Шифр Атбаш — это шифр подстановки, в котором алфавит зеркально отражается:
- А ↔ Я
- Б ↔ Ю
- В ↔ Э
- ...
- М ↔ Ь
- Н ↔ Ы

## Практическая реализация

### Программная реализация на Python

#### Шифр Цезаря (русский алфавит)

```python
def caesar_encrypt(text, shift):
    result = ""
    for char in text:
        if 'а' <= char <= 'я' or 'А' <= char <= 'Я':
            if char.isupper():
                base = ord('А')
                alphabet_size = 32
            else:
                base = ord('а')
                alphabet_size = 32
            shifted = (ord(char) - base + shift) % alphabet_size
            result += chr(shifted + base)
        else:
            result += char
    return result

def caesar_decrypt(text, shift):
    return caesar_encrypt(text, -shift)


Шифр Атбаш (русский алфавит)
def atbash_encrypt(text):
    result = ""
    for char in text:
        if 'а' <= char <= 'я':
            result += chr(ord('я') - (ord(char) - ord('а')))
        elif 'А' <= char <= 'Я':
            result += chr(ord('Я') - (ord(char) - ord('А')))
        else:
            result += char
    return result

def atbash_decrypt(text):
    return atbash_encrypt(text)


Результаты тестирования
Тестирование шифра Цезаря
Пример 1:

Исходный текст: "ПРИВЕТ"

Ключ: 3

Зашифрованный текст: "ТУЛЗЁХ"

Расшифрованный текст: "ПРИВЕТ"

Пример 2:

Исходный текст: "МИР"

Ключ: 5

Зашифрованный текст: "РНХ"

Расшифрованный текст: "МИР"

Тестирование шифра Атбаш
Пример 1:

Исходный текст: "АБВ"

Зашифрованный текст: "ЯЮЭ"

Расшифрованный текст: "АБВ"

Пример 2:

Исходный текст: "ПРИВЕТ"

Зашифрованный текст: "ПРЧЭЪГ"

Расшифрованный текст: "ПРИВЕТ"

Анализ криптостойкости
Уязвимости шифров
Шифр Цезаря:

Легко взламывается методом полного перебора (всего 31 возможный ключ для русского алфавита)

Уязвим к частотному анализу

Шифр Атбаш:

Является инволюцией (шифрование и дешифрование идентичны)

Также уязвим к частотному анализу

Имеет только 1 возможный ключ

Частотный анализ
Для русского языка наиболее частые буквы: О, Е, А, И, Н, Т. Анализируя частоту появления символов в шифротексте, можно определить соответствия и взломать шифр.

Заключение
В ходе лабораторной работы были успешно реализованы и протестированы два шифра простой замены. Шифр Цезаря продемонстрировал принцип сдвига алфавита, а шифр Атбаш — принцип зеркального отображения.

Основные выводы:

Шифры простой замены уязвимы к частотному анализу

Моноалфавитные подстановки не обеспечивают достаточной криптографической стойкости

Для современной защиты информации требуются более сложные алгоритмы

Оба шифра представляют в основном исторический и учебный интерес

Работа выполнена в полном соответствии с заданием лабораторной работы.